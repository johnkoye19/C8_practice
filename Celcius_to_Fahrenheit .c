#include <stdio.h>

int main() {
    /**
     * celcuis to Fahrenhei
     */
    float c, a = 30;
     
    //printf("Hello world");
    c = (1.8 * a) + 32;
    printf("%f degrees is equal to %f Fahrenheit", a, c);

    return (0);
}
